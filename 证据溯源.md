整体链路

提问 → ① 候选词切分 → ② 五表 LIKE 检索+评分 → ③ 图扩展 → ④ 配额截断 → ⑤ 证据JSON → ⑥ LLM 流式回答
① 候选词生成（build_retrieval_candidates）
提问先按标点切成短句，每句生成两类候选：

整句（≤16 字）：如 边骑车边写歌
3~8 字滑窗：如 边骑车、骑车边、车边写、骑车直播放歌 …
过滤掉停用词（这是/从哪/哪个群/帮我查 等）和纯标点，去重后 ≤120 条。3 字起步是因为 2 字词（写歌、直播）在中文里噪音太大，实测会让命中数从 6 条爆到 40+。

② 五表检索 + 评分
对每个候选词，逐表跑 LIKE '%词%' ESCAPE '\'（特殊字符已转义）：

表	查的字段	早停阈值
messages	content	累计命中 ≥40 行
events	value_text / key_field / key_field2 / payload（OR）	≥40
demands	name / note	≥40
summaries	content（记录命中位置，用于截片段）	≥40
reports	content（同上）	≥40
评分 = Σ 命中 gram 长度：长 gram 命中权重高，短 gram 只是辅助。命中行存 direct: true 标记。

③ 三层图扩展（解决「转述措辞 ≠ 原文措辞」）
这是核心。你的问题「骑车」来自周报 LLM 转述，原消息写的是「踩车」——只查 messages 表必挂。扩展链：

需求 → 事件 → 消息：周报命中 → 需求名命中 → events.demand_id → messages
同群邻近消息：直接命中的消息 ±180 秒内，按时间距离就近取 6 条（分数随距离衰减），把同一段对话的上下文带进来
需求兄弟扩展：同批次注册的需求（first_seen ±60s）——因为「自行车+AI写歌」「踩车加token」「直播放歌」是同一段对话里连续提出的，只命中一个就能把其余三个拉出来
最终这次验收问题：直接命中 6 条 → 扩展后 30 条证据（消息/事件/需求/周报）。

④ 配额截断
按类型配额（msg 25 / event 12 / demand 8 / report 5 / summary 5），同类内按 直接命中 > 近邻 > 得分 > 时间 排序，防止高频词命中挤掉溯源所需的周报/需求。总预算 60 条 / 12000 字。

⑤ 证据 JSON → ⑥ LLM
证据序列化成中文 key 的 JSON（群名/群编号/发送人/发送人ID/时间/内容），群编号通过 LEFT JOIN monitored_chats ON display_name = source_group 补上（messages 表只存群显示名）。

拼成 system(含证据+7条规则) + 近10轮历史 + 当前提问 调 deepseek 流式。规则强制：只依据证据回答、溯源逐条引用群/人/时间/原文/证据 id、相似消息必须全列、措辞不一致必须点明转述来源（如 report#2）。

为什么不用向量检索
数据量只有 ~2500 消息 / ~3000 事件，SQLite 全表 LIKE 是毫秒级。n-gram 子串匹配对中文短词、错别字（骑车/踩车）更抗揍，且零依赖、可解释。规模大了再换 FTS5/向量库不迟。